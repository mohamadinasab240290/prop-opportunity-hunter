1"}
name: Daily Report

on:
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Send test message
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" 
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" 
          -d text="✅ ربات چشم سوم کسب‌وکار با موفقیت فعال
